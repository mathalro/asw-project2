package sonc.quad;

/**
 * An object with x and y coordinates, each with its own getter.
 * 
 * @author Matheus Rosa
 * @version 1.0
 */
public interface HasPoint {
	double getX();

	double getY();
}